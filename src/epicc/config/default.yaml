app:
  title: TB Isolation Cost Calculator
  description: Streamlit-based simulation tool for tuberculosis (TB) isolation scenarios.

defaults:
  decimal_precision: 4
  ui_theme: light

model_paths:
  selected_path: models
  custom_path: custom_models
